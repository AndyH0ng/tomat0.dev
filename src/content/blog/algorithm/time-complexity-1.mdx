---
title: "시간 복잡도ㅇㅇ"
description: "Learn how to show code in your blog posts"
pubDate: "Dec 10 2025"
published: false
math: true
heroImage: "/src/assets/blog-placeholder-4.jpg"
tags: ["code", "markdown"]
---

## 시간 복잡도

### 3.1 - 1

$$
\max(f(n), g(n)) = \Theta(f(n) + g(n))
$$

$$
c_1(f(n) + g(n)) \leq \max(f(n), g(n)) \leq c_2(f(n) + g(n))
$$

$$
c_1 \leq \frac{ \max(f(n), g(n)) } {f(n) + g(n)} \leq c_2
$$

$c_1$과 $c_2$가 존재하므로 $\max(f(n), g(n)) = \Theta(f(n) + g(n))$

### 3.1 - 2

$$
